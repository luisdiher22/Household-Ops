package com.householdops.app.approval;
//Different statuses an approval request can have, such as PENDING, APPROVED, REJECTED, or EXPIRED.
public enum ApprovalStatus {
    PENDING,
    APPROVED,
    REJECTED,
    EXPIRED
}
